# carts
> cartridge files in the parent's folder, and what is installed

## TRY THIS
```
parent
carts
```
Shows the `.kdc` files waiting in the cartridge folder (and the folder's
real path), then the installed games.

## SEE ALSO
install, uninstall, share
