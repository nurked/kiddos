# cp
> copy a file

## WHAT IT DOES
Makes a copy. `cp from to`. If `to` is a folder, the copy goes inside it.

## TRY THIS
```
cp welcome.txt hello.txt
mkdir backup
cp hello.txt backup
cp -r backup backup2
```

## OPTIONS
- `-r` copy a whole folder

## SEE ALSO
mv, rm
