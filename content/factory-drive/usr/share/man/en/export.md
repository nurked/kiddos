# export
> set a variable, like export NAME=Sam

## WHAT IT DOES
A variable is a name that holds some words. `$NAME` gives them back.
`export` sets one. `env` lists them all.

## TRY THIS
```
export PET=cat
echo my $PET
env
```

## SEE ALSO
env, ksh
