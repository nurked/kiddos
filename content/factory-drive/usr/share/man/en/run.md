# run
> run a program file: run game.bas

## WHAT IT DOES
Runs a program. A `.bas` file runs in BASIC. Anything else runs as a
script, like typing `./name`.

## TRY THIS
```
run guess.bas
run ~/bin/hello
```

## SEE ALSO
basic, ksh, chmod
