# sort
> put lines in order

## WHAT IT DOES
Reads lines and prints them in alphabetical order.

## TRY THIS
```
ls /bin | sort -r
sort -n numbers.txt
```

## OPTIONS
- `-r` reverse (z to a)
- `-n` as numbers (so 9 comes before 10)
- `-u` drop repeated lines

## SEE ALSO
uniq, pipes, grep
