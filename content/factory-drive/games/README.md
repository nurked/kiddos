# Games

Games live here, one folder each. Type `games` to list them and
`play <name>` to start one. Every game is a text file you can read:
`cat /games/guess/guess.bas`, `cat /games/rogue/rogue.c`.

Make your own with `newgame <name>`. A parent can share it with
`share`, and install games from friends with `install`.
