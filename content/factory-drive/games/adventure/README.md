# The Drive Below

A cave adventure. Rooms are folders, things are files. You explore with the
same words you use everywhere else: `ls`, `cd`, `cat`, `mv`.

Start it with `play adventure`. It builds the cave inside your home folder
(`~/cave`) so you can change anything you find. If you make a mess, remove
the cave (`rm -r ~/cave`) and play again.
