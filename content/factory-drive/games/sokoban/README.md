# Sokoban

You are `@`. Push the boxes `[]` onto the targets `.`. You can push, not
pull, so think before you move. R restarts the level, ESC quits.

The levels are pictures in the DATA lines of `/games/sokoban/sokoban.bas`:
`#` wall, `$` box, `.` target, `@` you. Draw your own.
