# Snake

Arrow keys steer. Eat the `@` to grow. Walls and your own tail end the
game. ESC quits.

It is one BASIC file: `cat /games/snake/snake.bas`. The whole game is a
loop that reads a key, moves one step, and draws with `PUT`.
